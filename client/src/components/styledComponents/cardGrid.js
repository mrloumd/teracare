/* eslint-disable no-tabs */
import styled from 'styled-components';
import { Card } from 'antd';

const StyledCardGrid = styled(Card)`
	.ant-card-grid {
		width: 50%;
	}
`;

const StyledCardGridItem = styled(Card.Grid)`
	`;

export { StyledCardGrid, StyledCardGridItem };
